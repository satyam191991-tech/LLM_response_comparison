# LLM_response_comparison
Ask a question to a list of LLM's and stored each of their responses, then using another evaluator LLM, compare the response of all the LLM's to determine which one gave the best response.
